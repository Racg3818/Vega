{
  "name": "vega",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
